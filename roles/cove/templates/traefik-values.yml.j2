# Traefik Helm values — HTTP only, no SSH ingress
ports:
  web:
    port: 80
    exposedPort: 80
  websecure:
    port: 443
    exposedPort: 443

logs:
  general:
    level: INFO
  access:
    enabled: true
