# 2026-09-22 — Sashay final for #51

**Responding to:** operator approval (post review loop)

Sashay closed for issue #51 (NODE_EXTRA_CA_CERTS docs on ca.cove config page).

- Review loop closed: 1 blocking + 1 minor + 2 nits all addressed in 502fbc1
  (curl -kL, launch-environment wording, host-path annotation, matrix
  sad: executable).
- Test gate green twice after the fix: 442 passed, 25 deselected; rebased on
  fjl/main, up to date.
- PR #52 body rewritten to full status (findings, resolutions, test evidence,
  wheel-rebuild merge note). Chronicle thread backfilled in true order —
  convention corrected going forward: entries post to the thread as written.
- `WIP:` prefix removed on operator approval.

Merge follow-up (post-merge release action): sync compose into the wheel,
rebuild, reinstall cove; `cove up` re-renders config.html via bringup.yml.

This is the final chronicle entry for this sashay.