# C4 Diagrams: Dagu in Cove

Four states: current, Dagu MVP, Dagu v1, future (`*.app.cove`).

MVP and v1 are identical — no phasing needed within Dagu scope. The future state adds per-app provisioning but extends the topology rather than rearchitecting it.

## Current State (5 services)

```mermaid
graph TB
    subgraph Host["macOS Host"]
        Colima["Colima (Docker runtime)"]
    end

    subgraph Compose["Cove Compose Stack"]
        nginx["nginx<br/>TLS termination, routing, pages"]
        forgejo["Forgejo<br/>git, CI, registry<br/>SQLite"]
        vault["Vault<br/>secrets<br/>Raft"
        ]
        dnsmasq["dnsmasq<br/>wildcard DNS"]
        dnsproxy["dnsproxy<br/>DoH proxy"]
    end

    Operator(("Operator"))

    Operator -->|"https://git.cove"| nginx
    Operator -->|"https://vault.cove"| nginx
    nginx --> forgejo
    nginx --> vault
    nginx -->|"static files"| PagesData["~/Documents/cove-data/pages/"]

    forgejo -->|"raft"| ForgejoData["~/Documents/cove-data/forgejo/"]
    vault -->|"raft"| VaultData["~/Documents/cove-data/vault/"]

    dnsmasq -->|"*.cove → 127.0.0.1"| Operator
```

No scheduler, no object storage, no notification bus. Backups are manual. Health checks are manual (`cove status`).

## Dagu MVP / v1 (8 services)

```mermaid
graph TB
    subgraph Host["macOS Host"]
        Colima["Colima (Docker runtime)"]
    end

    subgraph Compose["Cove Compose Stack"]
        nginx["nginx<br/>TLS, routing, pages"]
        forgejo["Forgejo<br/>git, CI, registry<br/>SQLite"]
        vault["Vault<br/>secrets<br/>Raft"]
        dnsmasq["dnsmasq<br/>wildcard DNS"]
        dnsproxy["dnsproxy<br/>DoH proxy"]

        dagu["Dagu<br/>script scheduler<br/>file-backed, no DB"]
        coveTools["cove-tools<br/>Alpine + curl + openssl<br/>+ jq + python3 + mc"]
        minio["MinIO<br/>object storage<br/>S3 API"]
        ntfy["ntfy<br/>notification bus<br/>pub/sub HTTP"]
    end

    Operator(("Operator"))

    Operator -->|"https://git.cove"| nginx
    Operator -->|"https://vault.cove"| nginx
    Operator -->|"https://dag.cove"| nginx
    Operator -->|"https://s3.cove"| nginx
    Operator -->|"https://notify.cove"| nginx

    nginx --> forgejo
    nginx --> vault
    nginx --> dagu
    nginx --> minio
    nginx --> ntfy

    nginx -->|"static files"| PagesData["~/Documents/cove-data/pages/"]

    forgejo --> ForgejoData["~/Documents/cove-data/forgejo/"]
    vault --> VaultData["~/Documents/cove-data/vault/"]
    dagu --> DaguData["~/Documents/cove-data/dagu/"]
    minio --> MinioData["~/Documents/cove-data/minio/"]
    ntfy --> NtfyData["~/Documents/cove-data/ntfy/"]

    dagu -->|"runs as"| coveTools

    coveTools -->|"GET /api/healthz"| forgejo
    coveTools -->|"GET /v1/sys/health"| vault
    coveTools -->|"GET /v1/sys/storage/raft/snapshot"| vault
    coveTools -->|"PUT object"| minio
    coveTools -->|"POST /topic"| ntfy

    dnsmasq -->|"*.cove → 127.0.0.1"| Operator
```

Key properties:
- **No Docker socket access anywhere.** Dagu runs inside cove-tools. All DAG steps are `run:` commands in that image.
- **Dagu is an HTTP client.** It calls Forgejo API, Vault API, MinIO S3 API, ntfy HTTP API. It does not manage any container.
- **MinIO writes to disk.** Objects are real files under `~/Documents/cove-data/minio/`, caught by regular machine backups.
- **ntfy delivers via HTTP.** Operator subscribes via web UI or PWA at `notify.cove`. No macOS-specific notification path needed.
- **MVP = v1.** No phasing within Dagu scope. All three new services (Dagu, MinIO, ntfy) land together because they're interdependent: Dagu needs MinIO for storage, ntfy for notifications.

## Future State (platform: `*.app.cove`)

```mermaid
graph TB
    subgraph Host["macOS Host"]
        Colima["Colima (Docker runtime)"]
    end

    subgraph Compose["Cove Compose Stack"]
        nginx["nginx<br/>TLS, routing, pages<br/>+ *.app.cove routing"]
        forgejo["Forgejo<br/>git, CI, registry"]
        vault["Vault<br/>secrets + per-app credentials"]
        dnsmasq["dnsmasq<br/>wildcard DNS"]
        dnsproxy["dnsproxy<br/>DoH proxy"]

        dagu["Dagu<br/>script scheduler"]
        coveTools["cove-tools"]
        minio["MinIO<br/>object storage<br/>per-app buckets"]
        ntfy["ntfy<br/>notification bus<br/>per-app topics"]

        appProv["Per-app Provisioning<br/>(future: Forgejo Action or CLI)"]
    end

    Operator(("Operator"))

    Operator -->|"https://*.cove"| nginx
    Operator -->|"https://myapp.app.cove"| nginx

    nginx --> forgejo
    nginx --> vault
    nginx --> dagu
    nginx --> minio
    nginx --> ntfy
    nginx -->|"route by app slug"| AppServices["app containers<br/>(user-deployed)"]

    dagu -->|"runs as"| coveTools
    coveTools -->|"cove internal DAGs"| forgejo
    coveTools -->|"cove internal DAGs"| vault
    coveTools -->|"cove internal DAGs"| minio
    coveTools -->|"cove internal DAGs"| ntfy

    AppServices -->|"user app S3 calls"| minio
    AppServices -->|"user app notify calls"| ntfy
    AppServices -->|"user app vault creds"| vault

    appProv -->|"create bucket"| minio
    appProv -->|"create topic + token"| ntfy
    appProv -->|"store app credentials"| vault

    dnsmasq -->|"*.cove, *.app.cove"| Operator
```

Key differences from v1:
- **nginx routes `*.app.cove`** to user-deployed app containers (same pattern as current `*.pages.cove`).
- **Per-app provisioning** creates MinIO buckets, ntfy topics+tokens, and Vault credentials for each app.
- **User apps consume platform APIs** — S3, pub/sub, secrets — via the same endpoints Cove internals use.
- **No rearchitecture of the core.** Dagu, MinIO, ntfy, cove-tools are unchanged. The future state adds routing rules, provisioning logic, and app containers — it extends the topology rather than rearchitecting it.

## What This Validates

1. **MVP = v1.** No reason to phase Dagu separately from MinIO/ntfy. They're interdependent.
2. **The topology is forward-compatible.** `*.app.cove` adds routing + provisioning, doesn't touch the core services.
3. **No Docker socket, no proxy, no lethal trifecta** — in any state. Dagu is a pure HTTP client in all diagrams.
4. **MinIO and ntfy are platform services**, not Dagu dependencies. They appear in v1 serving Cove internals, extend naturally to serve user apps.
5. **The cove-tools image is stable across all states.** Same tools (curl, openssl, jq, python3, mc) serve Cove DAGs in v1 and user-app DAGs in the future state.